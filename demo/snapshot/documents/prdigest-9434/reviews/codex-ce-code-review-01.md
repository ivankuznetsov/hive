## High
- [x] AUTO-FIX: `lib/prdigest/runner.rb:14` exposes runtime credentials through object inspection: `Runner` retains the complete environment and `GitHub` retains `@token`, so default `inspect` output contains GitHub, Telegram, and unrelated process secrets in direct conflict with R9. <!-- triage: R9 and U5 define a credential-free inspection contract -->
- [x] AUTO-FIX: `lib/prdigest/github.rb:13` compounds Octokit's built-in retries with PRDigest's wrapper retries: a reproduced 500 response makes nine HTTP attempts for one logical request instead of the required maximum of three. <!-- triage: KTD5 fixes the limit at three total attempts -->
- [x] AUTO-FIX: `lib/prdigest/github.rb:137` does not retry real HTTP 429 responses: Octokit 9.2 maps 429 to `Octokit::ClientError`, which `transient?` rejects immediately without honoring `Retry-After`, violating R7's bounded rate-limit handling. <!-- triage: R7 and KTD5 specify bounded Retry-After handling -->
- [x] AUTO-FIX: `lib/prdigest/cli.rb:62` replaces rather than wraps Thor's command surface: `Prdigest::CLI.tasks` is empty, standard `--help`/`-h` and command help fail, and run-only options are silently accepted by other commands, breaking the R13/KTD8 compatibility contract. <!-- triage: R13 and U5 require preserving Thor commands and help compatibility -->
- [x] AUTO-FIX: `lib/prdigest/runner.rb:130` erases every remaining day when the over-cap skip checkpoint fails: `requested_days` is already populated but `failed_date` is nil, so the result incorrectly returns `remaining_days: []` and violates R10's stable progress envelope. <!-- triage: R10 and U5 define the remaining-days envelope -->
- [x] AUTO-FIX: `test/smoke/docker.sh:21` masks an unreadable production config mount: the smoke mounts a mode-0644 repository fixture, while the documented root-owned mode-0640 host file keeps a numeric GID that normally differs from the image's independently created `prdigest` group, causing the non-root container to fail at startup. <!-- triage: U6 requires production-equivalent ownership initialization -->
- [x] AUTO-FIX: `wiki/log.d/20260716-prdigest-v0-1-0.md:12` explicitly leaves mandatory release evidence unfinished: the authenticated GitHub boundary check, allowlisted Telegram smoke, and clean Ubuntu walkthrough required by R12 and the Verification Contract have not been performed. <!-- triage: R12 and the Verification Contract make these mandatory gates -->
- [x] AUTO-FIX: `test/runner_test.rb:116` does not cover AE4's full retry invariant: it checks only one failed invocation and never proves that a second invocation resends every chunk from the beginning before checkpointing the date. <!-- triage: AE4 specifies the exact missing retry assertion -->

## Medium
- [x] AUTO-FIX: `test/smoke/docker.sh:16` bypasses application state persistence: a shell `touch` plus explicit-date dry-run never constructs `State`, so the required container smoke can pass without proving atomic `state.json` creation, mode, ownership, or configured path. <!-- triage: U6 container smoke explicitly requires application state-path proof -->
- [x] AUTO-FIX: `lib/prdigest/runner.rb:77` ignores the renderer's settlement discriminator: any empty chunk array is checkpointed as intentional suppression even when `outcome` is unknown or inconsistent, permitting a malformed renderer result to silently settle an owed day. <!-- triage: R3 defines suppression as the only empty settlement outcome -->
- [x] AUTO-FIX: `test/telegram_test.rb:25` replaces the production transport in every delivery test: TLS peer/hostname verification, the fixed Bot API origin, and redirect refusal can regress while the credential-bearing Telegram suite remains green. <!-- triage: KTD7 and U4 require direct transport-security coverage -->
- [x] AUTO-FIX: `test/cli_test.rb:59` can pass after strict date validation is removed: the malformed-date case also lacks a Telegram token and asserts only the generic config error kind, so it fails earlier for an unrelated reason. <!-- triage: U5 requires an isolated malformed-date regression test -->

## Nit
