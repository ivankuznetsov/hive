# Digest LOC colors

Added line counts are green and deleted line counts are red in the Web digest.
The existing theme colors support both light and dark mode; plus/minus signs
keep the meaning visible without color. Saved text and statistics are unchanged.
