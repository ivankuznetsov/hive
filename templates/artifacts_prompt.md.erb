Stage 7 outcome evidence is controller-owned.

No single artifact agent may declare this stage complete. Hive launches fresh
inference, producer, and reviewer contexts using the dedicated role prompts,
validates the resulting requirement, proof bytes, hashes, traceability, and
semantic verdicts, then publishes the immutable accepted attempt itself.

Process exit and an agent-authored `<!-- COMPLETE -->` marker are never
publication authority.
