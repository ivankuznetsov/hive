{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:hive:schema:status-diagnose:v2",
  "title": "hive status --diagnose output (v2)",
  "description": "Emitted by `hive status --diagnose <slug> [--write] --json`. Either a SuccessPayload (local diagnostic plus optional artifact path) or an ErrorPayload (a Hive::Error envelope). Discriminate by `ok`. v2 adds task id and display_name fields.",
  "oneOf": [
    {
      "$ref": "#/$defs/SuccessPayload"
    },
    {
      "$ref": "#/$defs/ErrorPayload"
    }
  ],
  "$defs": {
    "SuccessPayload": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema",
        "schema_version",
        "ok",
        "slug",
        "id",
        "display_name",
        "task_folder",
        "diagnostic",
        "path"
      ],
      "properties": {
        "schema": {
          "const": "hive-status-diagnose"
        },
        "schema_version": {
          "const": 2
        },
        "ok": {
          "const": true
        },
        "slug": {
          "type": "string"
        },
        "id": {
          "type": [
            "integer",
            "null"
          ]
        },
        "display_name": {
          "type": [
            "string",
            "null"
          ]
        },
        "task_folder": {
          "type": "string"
        },
        "marker_summary": {
          "description": "Current marker name plus display attrs, used by bots and agents as a cheap evidence summary prefix. Optional (not required) so adding it stayed a non-breaking additive change per lib/hive.rb; null on a markerless task.",
          "type": [
            "string",
            "null"
          ]
        },
        "state_file": {
          "description": "Absolute path of the authoritative task state file the marker_summary was read from. Lets the bot evidence fallback pin the marker-tier source_path to the same file the CLI did. Optional/additive; absent on older binaries.",
          "type": [
            "string",
            "null"
          ]
        },
        "diagnostic": {
          "description": "Mirrors the Diagnostic shape under the current hive-status `tasks[].diagnostic`. Inlined here so the schema validates standalone (JSONSchemer + most validators reject external URN refs without a registry).",
          "oneOf": [
            {
              "$ref": "#/$defs/Diagnostic"
            },
            {
              "type": "null"
            }
          ]
        },
        "path": {
          "description": "Set only when `--write` was passed and the headless diagnose agent wrote diagnostics/red-status.md. Null otherwise.",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "ErrorPayload": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema",
        "schema_version",
        "ok",
        "error_class",
        "error_kind",
        "exit_code",
        "message"
      ],
      "description": "Emitted by the top-level rescue in Hive::Commands::Status#call when --diagnose fails (unknown slug, ambiguous slug, agent profile misconfigured, headless agent failed/timed out).",
      "properties": {
        "schema": {
          "const": "hive-status-diagnose"
        },
        "schema_version": {
          "const": 2
        },
        "ok": {
          "const": false
        },
        "error_class": {
          "type": "string"
        },
        "error_kind": {
          "type": "string",
          "enum": [
            "config",
            "internal",
            "error",
            "stale_marker",
            "in_flight",
            "slug_not_found",
            "ambiguous_slug"
          ]
        },
        "exit_code": {
          "type": "integer",
          "minimum": 0,
          "maximum": 255
        },
        "message": {
          "type": "string"
        },
        "candidates": {
          "type": "array",
          "description": "Present on AmbiguousSlug. Each candidate identifies a project + stage + folder.",
          "items": {
            "type": "object",
            "required": [
              "project",
              "stage",
              "folder"
            ],
            "properties": {
              "project": {
                "type": "string"
              },
              "stage": {
                "type": "string"
              },
              "folder": {
                "type": "string"
              }
            }
          }
        },
        "id": {
          "type": "string"
        },
        "path": {
          "type": "string"
        }
      }
    },
    "OutputReference": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "size", "sha256"],
      "properties": {
        "path": { "type": "string", "minLength": 1, "maxLength": 512 },
        "size": { "type": "integer", "minimum": 0 },
        "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      }
    },
    "AttemptDiagnosticProvider": {
      "oneOf": [
        { "type": "null" },
        {
          "type": "object", "additionalProperties": false,
          "required": ["name", "failure_class", "retry_hint", "provenance"],
          "properties": {
            "name": { "type": ["string", "null"], "maxLength": 128 },
            "failure_class": { "type": ["string", "null"], "maxLength": 128 },
            "retry_hint": { "type": ["string", "null"], "maxLength": 128 },
            "provenance": { "type": ["string", "null"], "maxLength": 128 }
          }
        }
      ]
    },
    "Diagnostic": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "summary",
        "detail",
        "source",
        "source_path",
        "artifact_paths",
        "generated_by",
        "marker_signature",
        "suggested_next_action",
        "updated_at"
      ],
      "properties": {
        "summary": {
          "description": "Single-line operator summary. Producer caps at 120 chars (Hive::TaskAction::Diagnostic::SUMMARY_MAX); see Hive::TaskAction#diagnostic.",
          "type": "string",
          "maxLength": 120
        },
        "detail": {
          "description": "Multi-line operator detail. Producer caps at 4000 chars (Hive::TaskAction::Diagnostic::DETAIL_MAX); see Hive::TaskAction#diagnostic. On the on-disk evidence fallback path (nil production diagnostic) this is instead a one-line `Label: path` pointer to the backing artifact/log/state file, not a multi-line excerpt.",
          "type": "string",
          "maxLength": 4000
        },
        "source": {
          "type": "string",
          "enum": [
            "marker",
            "artifact"
          ]
        },
        "source_path": {
          "type": [
            "string",
            "null"
          ]
        },
        "artifact_paths": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "maxItems": 20
        },
        "generated_by": {
          "type": "string",
          "enum": [
            "local",
            "claude",
            "codex",
            "pi",
            "grok",
            "opencode"
          ]
        },
        "marker_signature": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "suggested_next_action": {
          "oneOf": [
            {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "kind",
                "command"
              ],
              "properties": {
                "kind": {
                  "type": "string",
                  "enum": [
                    "retry",
                    "clear_marker",
                    "manual_fix"
                  ]
                },
                "command": {
                  "type": [
                    "string",
                    "null"
                  ]
                }
              }
            },
            {
              "type": "null"
            }
          ]
        },
        "updated_at": {
          "type": "string",
          "format": "date-time"
        },
        "code": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$", "maxLength": 128 },
        "owner": { "enum": ["agent", "provider", "hive", "operator", "unknown"] },
        "workflow": { "const": "patrol_fix" },
        "stage": { "type": "string", "minLength": 1, "maxLength": 128 },
        "phase": { "type": "string", "minLength": 1, "maxLength": 128 },
        "status": { "type": "string", "minLength": 1, "maxLength": 128 },
        "agent_reason": { "type": ["string", "null"], "maxLength": 128 },
        "exit_code": { "type": ["integer", "null"] },
        "timed_out": { "type": "boolean" },
        "cancelled": { "type": "boolean" },
        "signal": { "type": ["string", "null"], "maxLength": 128 },
        "provider": { "$ref": "#/$defs/AttemptDiagnosticProvider" },
        "attempt_id": { "type": "string", "minLength": 1, "maxLength": 128 },
        "correlation_id": { "type": "string", "minLength": 1, "maxLength": 128 },
        "task_generation": { "type": "string", "minLength": 1, "maxLength": 128 },
        "diagnostic_digest": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "diagnostic_reference": { "$ref": "#/$defs/OutputReference" },
        "log_reference": { "$ref": "#/$defs/OutputReference" },
        "report_status": { "type": "string", "minLength": 1, "maxLength": 128 },
        "report_parser": { "type": ["string", "null"], "maxLength": 128 },
        "firewall_status": { "type": "string", "minLength": 1, "maxLength": 128 },
        "firewall_restoration": { "type": ["string", "null"], "maxLength": 128 },
        "custody_status": { "type": "string", "minLength": 1, "maxLength": 128 },
        "transport_status": { "enum": ["valid", "missing", "oversized", "malformed", "duplicate", "unavailable"] },
        "redaction_status": { "enum": ["applied", "omitted", "failed"] },
        "secret_policy_version": { "type": "integer", "minimum": 1 }
      }
    }
  }
}
