## [2026-07-19T00:01:29Z] feature - workflow-derived Kanban board

**Action:** Added an equal first-class Kanban view beside the grid, with a
browser-selectable default, project/workflow bands, descriptor-ordered stages,
URL filters/grouping, live reconciliation, a shared task drawer, accessible
Move-to forms, and optional desktop fine-pointer drag.

**Safety:** Status owns semantic fingerprints, dominant states, allowed
transitions, blocked explanations, operational chips, card digests, and
workflow-terminal identity. Web mutations revalidate under the project commit
lock; successful synchronous moves commit their `operator_action` audit with
the folder move, queued moves retain daemon-time preconditions, and denials
stay out of task history. Rails adds no workflow database.

**Coverage:** Added core schema/fingerprint/dependency/queue/audit tests, Rails
route/filter/drawer/transition/performance tests, and Playwright coverage for
keyboard/mobile/live/menu/drag behavior. Grid-specific browser tests now use
the explicit `/grid` route. The golden-path E2E now starts its real daemon from
the root lockfile instead of inheriting the activated web bundle, and the web
security lock was refreshed to patched compatible gems. Updated
[[commands/web]], [[commands/status]], [[state-model]], [[modules/events]],
[[testing]], and [[gaps]].
