# Print complete circuit repair tokens

Human `hive circuits` inspection now includes
`repair_last_verified_generation` alongside the journal epoch and corruption
fingerprint. The default output contains every field required for a safe scoped
reset.
