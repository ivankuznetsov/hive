You are running the review pass of stage 4-execute for project <%= project_name %>.

Worktree (cwd): <%= worktree_path %>
Task folder (--add-dir): <%= task_folder %>
Default branch (compare against): <%= default_branch %>
Pass: <%= pass %>

Behavior:

1. Use the `/compound-engineering:ce-review` skill on the diff
   `git diff <%= default_branch %>..HEAD` in the current worktree.
2. Output structured findings to <%= task_folder %>/reviews/ce-review-<%= "%02d" % pass %>.md
   using GFM checkboxes grouped by severity:

   ```
   ## High
   - [ ] <finding>: <one-line justification>

   ## Medium
   - [ ] <finding>: <one-line justification>

   ## Nit
   - [ ] <finding>: <one-line justification>
   ```

   Use `[ ]` (unchecked) for every finding. The user will tick `[x]` for the
   ones they want addressed in the next pass.

Constraints:
- Write only to <%= task_folder %>/reviews/ce-review-<%= "%02d" % pass %>.md.
- Do not edit any code file in the worktree.
- Do not edit task.md, plan.md, worktree.yml, or any other review file.
  The hive runner sets the task.md marker after counting your findings — you
  must not write task.md yourself.
