# Completed workflow board

Adds an on-demand Done board backed by archived task records. This makes
completed Architecture tasks visible in their new `8-done` stage without
expanding the active feed or rerunning document writers.
