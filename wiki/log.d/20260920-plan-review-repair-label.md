# Plan review repair label

Replaced the configuration-specific task label with `Plan review needs repair` in task actions and status ordering. Added a regression for storage failures during reviewer worktree creation. Retry and approval behavior is unchanged.
