## High

- [x] AUTO-FIX: Bound GitHub requests to three real HTTP attempts (`lib/prdigest/github.rb:13`): Octokit's default Faraday retry middleware is nested inside PRDigest's three-attempt loop, and repeated 500 responses were reproduced as nine requests. <!-- triage: KTD5 fixes the total-attempt contract -->
- [x] AUTO-FIX: Handle real GitHub rate-limit responses (`lib/prdigest/github.rb:137`): Octokit maps HTTP 429 to a generic client error that this retry policy refuses, while reset-only responses ignore `X-RateLimit-Reset`, violating the required bounded server-aware retry contract. <!-- triage: KTD5 defines bounded server-aware retry behavior -->
- [x] AUTO-FIX: Reject pagination drift before checkpointing (`lib/prdigest/github.rb:45`): the loop stops on raw item count, so a repeated PR on a later page can replace an omitted PR, produce a duplicate digest, and silently settle an incomplete day. <!-- triage: KTD5 requires complete pagination before settlement -->
- [x] AUTO-FIX: Make post-rename state failures consistent with settlement (`lib/prdigest/state.rb:41`): `chmod` or directory `fsync` can fail after the new checkpoint is already visible, causing Runner to report the date unsettled even though the next run skips it. <!-- triage: KTD2 and R3 define fail-closed settlement semantics -->
- [x] AUTO-FIX: Redact runtime object inspection (`lib/prdigest/runner.rb:14`, `lib/prdigest/github.rb:12`): Runner retains the complete environment and GitHub retains the token without redacted `inspect` methods, so ordinary Ruby inspection exposes credentials. <!-- triage: R9 requires credentials absent from inspection surfaces -->
- [x] AUTO-FIX: Prevent local operator data from entering container layers (`Dockerfile:11`, `.dockerignore:1`): broad `COPY . .` plus the new incomplete denylist still includes untracked config, state, and private-metadata files in the image build context. <!-- triage: U6 requires deterministic secret-free packaging -->
- [x] AUTO-FIX: Restore the promised Thor command surface (`lib/prdigest/cli.rb:62`): the custom dispatcher leaves `Prdigest::CLI.tasks` empty and rejects `--help`/`-h`, breaking the plan-required `run`, `serve`, `version`, and help compatibility. <!-- triage: R13 and U5 explicitly preserve Thor compatibility -->
- [x] AUTO-FIX: Preserve remaining work on pre-fetch state failures (`lib/prdigest/runner.rb:129`): a skipped-prefix checkpoint failure occurs after `requested_days` is populated but before `failed_date` is set, so the result incorrectly reports `remaining_days: []`. <!-- triage: R10 defines the complete failure envelope -->
- [x] AUTO-FIX: Pace Telegram messages across catch-up days (`lib/prdigest/telegram.rb:34`): Runner reuses one client but calls `deliver` once per date, resetting the chunk index and omitting the required delay between consecutive day batches. <!-- triage: KTD7 assigns per-chat pacing to the client -->
- [x] AUTO-FIX: Report durable progress on human-mode failures (`lib/prdigest/cli.rb:117`): partial failures print only the error, so settled/skipped/remaining dates disappear from normal systemd output and a cleared pending-skip notice may never be surfaced. <!-- triage: U5 requires concise human progress plus errors -->
- [ ] ESCALATE: Complete the mandatory production validation evidence (`wiki/log.d/20260716-prdigest-v0-1-0.md:12`): the diff explicitly leaves the authenticated GitHub check, allowlisted Telegram smoke, clean Ubuntu walkthrough, and remaining independent review unfinished, so R12 and the Definition of Done are unmet. <!-- triage: unresolved after checking plan R12/U6, brainstorm A9, task metadata, release log, and available credentials -->

## Medium

- [x] AUTO-FIX: Exercise application checkpointing in the container smoke (`test/smoke/docker.sh:16`): a shell `touch` followed by explicit-date dry-run bypasses State entirely and does not prove the packaged non-root process can create the configured mode-0600 checkpoint. <!-- triage: U6 test scenario 4 requires application-owned state proof -->
- [x] AUTO-FIX: Enforce Telegram's retry wait as a cumulative budget (`lib/prdigest/telegram.rb:112`): each delay is capped independently, allowing two 31-second waits to exceed the required shared 60-second maximum. <!-- triage: KTD7 specifies one shared 60-second wait budget -->
- [x] AUTO-FIX: Test the production Telegram transport defenses (`test/telegram_test.rb:26`): every test injects `FakeTransport`, so TLS peer/hostname verification and redirect non-following can regress while CI remains green. <!-- triage: U4 test scenario 9 explicitly requires transport-defense coverage -->

## Nit
