# Finish the PR 1148 simplification pass

The final review cleanup removes the remaining duplicated and defensive
mechanism without changing workflow authority. Council rounds now have one
pre-spawn budget boundary, plan-review state transitions share one publisher,
dispatch-request queries share one ENOENT-tolerant iterator, and the dead
brainstorm predicate wrapper is gone.

Artifact Firewall `max_entries` is now the aggregate inventory bound across
protected anchors, required outputs, and writable roots instead of three
independent allowances. Smaller seams were also collapsed: one Pi refusal
fixture feeds the runtime and Hive boundary tests, provider completion evidence
uses one table-driven regression, projection-binding sidecar reads share one
helper, exact guardrail waivers use a Set, and Claude shared sessions no longer
accept OpenCode-only keywords merely to reject them.

The cleanup also removes unreachable fallback branches in plan decoding,
dependency adoption, attempt-loss dispatch, recovery row access, and healer
ladder selection. Focused tests pin the aggregate firewall limit, exact council
round count and resume behavior, queue recovery queries, plan-review
transitions, brainstorm state, provider extraction, proof sidecars, and
guardrail waivers.
