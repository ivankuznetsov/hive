## 2026-07-28: Close Review 02 OpenClaw creator-proof gaps

**Action:** Replaced the creator proof's Marshal/supervisor path with strict
bounded JSON/Base64 framing and an independently live containment owner;
replaced Ruby fixture authoring with a digest-bound driver over OpenClaw's
public config-schema and agent-harness exports; added exact-runtime direct-tool
policy controls plus independently observed
filesystem/network receipts; made every gateway attempt durable and terminally
poisoning after denial/failure; bound complete read-only installed trees,
launchers, and interpreters; and initialized typed evidence before every
ordinary hosted preparation partition. The live creator prompt now names the
exact creation commands and requires deletion of unused blank-scaffold files,
and the JSON frame codec normalizes diagnostic strings to valid UTF-8 while
keeping captured binary streams Base64-encoded.

**Evidence:** Focused tests cover malformed/truncated/oversized/trailing frames,
worker SIGSTOP/SIGKILL with escaped descendants, wrong-order/digest/budget/
candidate/extra gateway attempts, inner-runtime/interpreter mutation, all typed
preparation failure partitions, missing effect observations, and the full fake
OpenClaw nine-command/dynamic-slug proof. The public-export contract resolves
exactly `apply_patch/edit/exec/read/write` and executes successful workspace
read/write/edit/apply-patch plus `hive version` controls. Unchanged sentinels
prove denials for sibling write/edit/apply-patch and absolute, redirected, and
chained exec attempts. Deterministic authoring is explicitly tool-surface
evidence with no model loop, and scoped unauthorized-effect evidence retains
the pinned beta's outside-read skill-root caveat. Socket receipts are retained
as unattributed observations rather than being inferred as provider transport
or adjudicated as authorized/unauthorized effects.

**Remaining gap:** No credentialed protected-main `live-workflow-creator` run
was triggered. Trusted live attestation still requires the operator-controlled
hosted proof described in `wiki/gaps.md`.
