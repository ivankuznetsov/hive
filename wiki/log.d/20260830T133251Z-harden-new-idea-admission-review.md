# 2026-08-30 — Harden new-idea admission review boundaries

New-idea admission now carries Snapshot-classified recovery instead of exposing
raw project errors to Update, and scoped or filtered snapshots preserve the
registry-wide admission authority. Blank registry identities fail closed with
distinct repair feedback.

Submit-time revalidation preserves disappearance when a pinned sole project is
removed, uses truthful empty-picker recovery copy, and never replays an older
entry-time resolution. The existing attachment smoke harness remains unchanged.
