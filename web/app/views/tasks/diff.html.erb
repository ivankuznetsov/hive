<% content_for(:title, "Hive web — diff #{@row["slug"]}") %>
<div class="task-header">
  <h1>Diff</h1>
  <%= stage_badge(@row["stage"]) %>
</div>
<p class="task-slug"><%= @project["name"] %> · <%= @row["slug"] %></p>
<p><%= link_to "← Back to task", task_path(@project["name"], @row["slug"]) %></p>

<% if @diff.strip.empty? %>
  <div class="empty-state"><p>The worktree has no uncommitted changes.</p></div>
<% else %>
  <% if @diff_truncated %>
    <p class="flash-alert">Diff truncated to the first 512&nbsp;KB — inspect the worktree directly for the full change.</p>
  <% end %>
  <pre><%= @diff %></pre>
<% end %>
