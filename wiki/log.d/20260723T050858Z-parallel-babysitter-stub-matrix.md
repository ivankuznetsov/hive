## [2026-07-23T05:08:58Z] test - parallelize the babysitter stub matrix

**Action:** The large set of independent Git/GitHub allow/deny cases in `babysitter/dry_run_env_test.rb` now launches through a bounded 12-worker pool instead of serially. Every case still crosses the real shell/Ruby stub process boundary and writes through the production audit-log path; subprocess capture happens in workers, while all exit, stderr, skipped-command, and exact passthrough assertions remain on the main Minitest thread. Deferred negative checks compare complete invocation-log lines so a safe later command such as `git status --short` cannot be mistaken for a skipped shorter command such as `git status`.

**Coverage:** The profiled matrix fell from 44.77s to 7.62s while retaining 1,376 assertions. The full security file passes at seeds `3691` and `23422` with 66 runs and 2,131 assertions, and RuboCop reports no offenses. The exact seed-`23422` suite passes in 573.610908s versus the preceding same-seed 658.145470s: 10,130 runs, 141,952 assertions, 0 failures/errors, 7 skips, 1,126 process results, and 55,153/55,153 lines. Updated [[testing]]; production stub behavior and known uncertainties are unchanged.
