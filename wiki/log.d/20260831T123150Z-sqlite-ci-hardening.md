---
title: Harden SQLite cutover CI fixtures
date: 2026-08-31
---

- Activated a real SQLite runtime control plane in isolated Rails and CLI E2E
  homes before project registration, and retired system-test projects through
  the production registry API between examples.
- Made durable workers reopen the activated default control plane and retained
  all loaded gem require paths when spawning the scrubbed terminal recorder.
- Kept the provider-free Web golden path provider-free by emitting a complete,
  locally scoped plan that satisfies the real skip-review classifier.
- Updated dispatch, diagnostic-log, incident, installer, packaged-Web, and
  security fixtures to exercise the SQL-backed contracts instead of retired
  filesystem or implicit-migration behavior.
- Removed a load-sensitive process-supervisor test race by allowing Ruby to
  start before proving that continuous output cannot starve the bounded
  timeout and reap contract.
- Made module-hook attempt rows bind directly to their registered SQL project
  while leaving `task_id` empty, so the first-class module subject no longer
  depends on a test-only fabricated task identity.
- Made real-process scenario seeds publish stable task metadata and converted
  the remaining module/provider helpers to explicit activated SQL repositories.
- Registered command and daemon module projects in the same isolated SQL
  repository used by their hook attempts, instead of relying on file-only
  project fixtures that the production admission contract correctly rejects.
- Kept malformed routing, live-admission, and failure-cohort JSON behind the
  Attempts repository's typed error boundary instead of leaking `ArgumentError`.
- Normalized operator generation input before entering Sequel's transaction
  boundary, preserving the provider-health stale-generation error contract.
- Kept invalid sealed-payload size expectations behind the typed payload
  integrity boundary instead of leaking `TypeError`.
