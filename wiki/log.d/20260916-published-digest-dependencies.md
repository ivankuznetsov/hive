# Published digest dependencies

Hive now requires PRDigest 0.4.0 and agent-cli-runtime 0.2.4, both published and
verified through clean RubyGems installations. The root and Web bundles resolve
these versions without a local PRDigest worktree override.
