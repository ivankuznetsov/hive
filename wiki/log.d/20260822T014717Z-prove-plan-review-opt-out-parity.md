# Prove benchmark plan-review opt-out parity live

- Closed the live-evidence gap after the Ox Alpha campaign completed six Pi and
  six OpenCode cells with `plan_review.enabled: false` in both harnesses.
- All 12 cells reached generated terminal results with zero pending and zero
  failed rows, proving the benchmark-local grant and direct plan-to-execute
  transition across both agent profiles.
- The opt-out remains process-granted and benchmark-local; ordinary Hive
  configuration still rejects disabled plan review.
